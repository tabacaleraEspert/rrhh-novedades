// Copiá este archivo como "appsettings.secrets.local.json" (está gitignoreado) y completá los valores.
// Program.cs lo carga automáticamente si existe. NO commitear el archivo real.
{
  // Credenciales de la cuenta Twilio de ChatbotCobros (ver Azure App Settings / Key Vault de ese proyecto).
  "Twilio": {
    "AccountSid": "AC...",
    "AuthToken": "..."
  },
  // API key de Humand. UseMock=false para pegarle a la API real en vez de usar datos simulados.
  "Humand": {
    "ApiKey": "...",
    "UseMock": false
  }
}
